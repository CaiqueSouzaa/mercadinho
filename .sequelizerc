const { resolve } = require('path');

module.exports = {
    config: resolve(__dirname, 'api', 'bin', 'config', 'database.cjs'),
    'migrations-path': resolve(__dirname, 'api', 'bin', 'database', 'migrations'),
    'seeders-path': resolve(__dirname, 'api', 'bin', 'database', 'seeds'),
};
